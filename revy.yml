apiVersion: reviewpad.com/v1alpha

rules:
  tautology:
    kind: patch
    description: Tautology
    spec: 1 == 1

protectionGates:
  ship:
    description: Ship mode
    patchRules:
      - rule: tautology
    actions:
      - $merge("rebase")
